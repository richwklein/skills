{
  "$comment": "File-level classification for template drift audits. The template repo's file tree is the canonical truth; this config controls which files to skip, which need only presence checks, and which require schema validation.",
  "ignore": [".git", "README.md"],
  "ignore_prefixes": [".git/", ".claude/", "src/"],
  "presence_only": [
    "CODE_OF_CONDUCT.md",
    "package.json",
    "package-lock.json",
    "astro.config.ts",
    "vitest.setup.ts",
    ".release-please-manifest.json"
  ],
  "schema_checks": [
    {
      "path": "package.json",
      "required_scripts": [
        "dev", "build", "preview", "lint", "lint:fix",
        "format", "format:fix", "test", "test:coverage", "verify"
      ]
    }
  ]
}
